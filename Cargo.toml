[package]
name = "tsp-recocido"
version = "0.1.0"
edition = "2024"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
rand = "0.9"
serde = { version = "1.0.229", features = ["derive"] }
toml = "1.1.6"
